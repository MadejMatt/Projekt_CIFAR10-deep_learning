# --- PyTorch (GPU) ---------------------------------------------------------
# UWAGA: RTX 5070 to architektura Blackwell (sm_120). Domyslne wheele torch
# (cu121/CPU) NIE maja kerneli dla sm_120 i daja "no kernel image is available"
# albo cichy fallback na CPU. Zainstaluj torch >= 2.7 zbudowany pod CUDA 12.8:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# Dopiero potem reszta zaleznosci ponizej (pip install -r requirements.txt).
torch>=2.7
torchvision>=0.22

# --- Reszta ----------------------------------------------------------------
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
jupyter
ipykernel
